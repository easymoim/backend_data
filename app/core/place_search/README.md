# 🍽️ Place Search - 모임 장소 추천 시스템

모임 참가자들의 위치, 선호도를 분석하여 최적의 장소를 추천하는 AI 기반 장소 추천 시스템입니다.

---

## 📌 개요

이 모듈은 3단계 파이프라인을 통해 모임에 적합한 장소를 추천합니다:

```
[1단계] 데이터 수집 → [2단계] 장소 검색 → [3단계] LLM 추천
```

---

## 🛠️ 기술 스택

| 구분 | 기술 |
|------|------|
| **Language** | Python 3.11+ |
| **Framework** | Pydantic (데이터 검증) |
| **외부 API** | Kakao Local API (장소 검색), Daum Search API (블로그 정보) |
| **LLM** | Google Gemini 2.0 Flash |
| **비동기** | asyncio, httpx |

---

## 📁 모듈 구조

```
place_search/
├── __init__.py           # 모듈 진입점 및 export
├── schemas.py            # Pydantic 스키마 정의
├── kakao_client.py       # 카카오 API 클라이언트
├── data_collector.py     # 1단계: 데이터 수집 및 분석
├── keyword_generator.py  # 검색 키워드 생성
├── place_searcher.py     # 2단계: 장소 검색 및 필터링
├── llm_recommender.py    # 3단계: LLM 기반 추천
├── station_utils.py      # 지하철역 유틸리티
└── README.md             # 이 문서
```

---

## 🔄 추천 프로세스

### 전체 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                     📊 1단계: 데이터 수집                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 참가자 위치   │  │ 참가자 선호도 │  │ 모임 정보    │          │
│  │ (location)   │  │ (preference) │  │ (purpose)    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         └─────────────────┼─────────────────┘                   │
│                           ▼                                     │
│              ┌────────────────────────┐                         │
│              │  중심 위치 계산         │                         │
│              │  선호도 집계 (가중치)   │                         │
│              │  검색 키워드 생성       │                         │
│              └────────────┬───────────┘                         │
└───────────────────────────┼─────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     🔍 2단계: 장소 검색                          │
│              ┌────────────────────────┐                         │
│              │    카카오 Local API    │                         │
│              │  - 키워드 검색         │                         │
│              │  - 카테고리 검색       │                         │
│              └────────────┬───────────┘                         │
│                           ▼                                     │
│              ┌────────────────────────┐                         │
│              │  결과 병합 & 중복 제거  │                         │
│              │  거리순 정렬           │                         │
│              └────────────┬───────────┘                         │
└───────────────────────────┼─────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     🤖 3단계: LLM 추천                           │
│              ┌────────────────────────┐                         │
│              │  Daum 블로그 검색      │                         │
│              │  (상세 정보 수집)      │                         │
│              └────────────┬───────────┘                         │
│                           ▼                                     │
│              ┌────────────────────────┐                         │
│              │   Gemini LLM 추천      │                         │
│              │  - 선호도 가중치 반영  │                         │
│              │  - 매칭 점수 계산      │                         │
│              │  - 추천 이유 생성      │                         │
│              └────────────┬───────────┘                         │
└───────────────────────────┼─────────────────────────────────────┘
                            ▼
                   📍 최종 추천 결과
```

---

## 📥 인풋 (Input)

### 장소 선택 방식 (3가지)

| 방식 | 값 | 설명 |
|------|-----|------|
| 중간위치 | `center_location` | 참가자들 위치의 중심점 기반 |
| 선호 지역 | `preference_area` | 투표로 선정된 구/동 기반 |
| 선호 지하철역 | `preference_subway` | 투표로 선정된 역 근처 |

### 인풋 데이터 구조

```python
# 1. 모임 목적
purpose: str  # "dining", "cafe", "drink", "etc"

# 2. 참가자 위치 (center_location 방식)
locations: [
    {"address": "서울 강남구 역삼동", "district": "강남구"},
    {"address": "서울 서초구 서초동", "district": "서초구"},
]

# 3. 참가자 선호도
preferences: [
    {
        "food_types": ["korean", "meat"],      # 음식 종류
        "atmospheres": ["quiet", "cozy"],      # 분위기
        "conditions": ["parking", "group_friendly"]  # 조건
    },
    ...
]

# 4. 장소 선택 방식별 추가 정보
location_choice_type: "preference_subway"
preferred_station: "홍대입구"           # preference_subway 방식
station_votes: {"홍대입구": 5, "강남": 2}

preferred_district: "마포구"            # preference_area 방식
district_votes: {"마포구": 4, "강남구": 2}

# 5. 기타
expected_count: 5  # 예상 참가자 수
top_n: 3           # 추천 장소 개수
```

### 선호도 옵션

**음식 종류 (FoodType)**
```
korean, japanese, chinese, western, asian, meat, seafood, chicken, pizza, cafe, bar, etc
```

**분위기 (AtmosphereType)**
```
quiet, lively, romantic, modern, traditional, cozy, spacious, private
```

**조건 (ConditionType)**
```
parking, private_room, group_friendly, pet_friendly, wheelchair, reservation, late_night
```

---

## 📤 아웃풋 (Output)

### 최종 결과 구조

```python
{
    "context": MeetingContext,           # 모임 컨텍스트
    "keywords": [SearchKeyword, ...],    # 사용된 검색 키워드
    "places": [KakaoPlaceResult, ...],   # 검색된 전체 장소
    "recommendations": LLMRecommendationResult  # LLM 추천 결과
}
```

### LLM 추천 결과 (place_candidate.location JSON)

```json
{
  "recommendations": [
    {
      "rank": 1,
      "place_id": "18257217",
      "place_name": "강강술래 홍대점",
      "reason": "3명이 선호하는 한식, 활기찬 분위기를 원하는 다수의 참가자에게 적합합니다.",
      "match_score": 85,
      "matched_preferences": ["한식", "활기찬/왁자지껄한", "단체 이용 가능"],
      "address": "서울 마포구 잔다리로6길 25",
      "latitude": 37.5523564,
      "longitude": 126.9205691,
      "phone": "02-3143-6635",
      "place_url": "http://place.map.kakao.com/18257217",
      "category": "한식",
      "distance": 575
    },
    ...
  ],
  "summary": "참가자들의 선호도를 종합적으로 고려하여 한식, 단체 이용 가능 여부를 중심으로 장소를 추천했습니다.",
  "model_used": "gemini-2.0-flash",
  "search_keywords": ["마포구 한식 맛집", "마포구 회식", "마포구 단체 한식"],
  "total_candidates": 45
}
```

---

## 🚀 사용법

### 기본 사용 (full_recommendation_pipeline)

```python
import asyncio
from app.core.place_search import full_recommendation_pipeline

async def main():
    result = await full_recommendation_pipeline(
        purpose="dining",
        locations=[{"address": "서울 강남구 역삼동", "district": "강남구"}],
        preferences=[
            {"food_types": ["korean"], "atmospheres": ["quiet"]},
            {"food_types": ["korean", "meat"], "atmospheres": ["lively"]},
        ],
        expected_count=5,
        top_n=3,
        location_choice_type="center_location",
    )
    
    # 추천 결과
    for rec in result["recommendations"].recommendations:
        print(f"{rec.rank}. {rec.place_name} ({rec.match_score}점)")
        print(f"   이유: {rec.reason}")

asyncio.run(main())
```

### 선호 지역 방식

```python
result = await full_recommendation_pipeline(
    purpose="dining",
    locations=[],  # 위치 정보 없어도 됨
    preferences=[{"food_types": ["korean"]}],
    location_choice_type="preference_area",
    preferred_district="마포구",
    district_votes={"마포구": 4, "강남구": 2},
)
```

### 선호 지하철역 방식

```python
result = await full_recommendation_pipeline(
    purpose="dining",
    locations=[],
    preferences=[{"food_types": ["japanese"]}],
    location_choice_type="preference_subway",
    preferred_station="홍대입구",
    station_votes={"홍대입구": 5, "강남": 2},
)
```

---

## ⚙️ 환경 설정

### 필수 환경변수 (.env)

```bash
KAKAO_REST_API_KEY=your_kakao_api_key
GEMINI_API_KEY=your_gemini_api_key
```


## 🔑 핵심 기능

### 1. 선호도 가중치 반영

참가자들의 선호도를 집계하여 **투표 수(가중치)**를 LLM에 전달합니다.

```
- 선호 음식: 한식(3명), 일식(2명), 아시안(1명)
- 선호 분위기: 활기찬(4명), 아늑한(1명), 모던(1명)
- 필요 조건: 심야영업(2명), 단체이용가능(2명)
```

### 2. 블로그 정보 수집

Daum 블로그 검색을 통해 각 장소의 **상세 정보**를 수집하여 LLM에 제공합니다.

```
- 특징 키워드: 룸, 단체, 예약, 주차
- 블로그 리뷰 요약: "조용하고 깔끔한 분위기에서..."
```

### 3. 지능적 키워드 생성

선호도를 기반으로 **효과적인 검색 키워드**를 자동 생성합니다.

```
[1] 마포구 한식 맛집
[2] 마포구 회식
[2] 마포구 단체 한식
[3] 마포구 맛집
```

---

## 📊 데이터 플로우

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Meeting   │     │ Participant │     │    Vote     │
│  - purpose  │────▶│  - location │────▶│  - station  │
│  - choice   │     │  - pref     │     │  - area     │
└─────────────┘     └─────────────┘     └─────────────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           ▼
                 ┌─────────────────┐
                 │  PlaceSearch    │
                 │  Pipeline       │
                 └────────┬────────┘
                          ▼
              ┌───────────────────────┐
              │   place_candidate     │
              │   .location (JSON)    │
              │   - recommendations   │
              │   - summary           │
              │   - search_keywords   │
              └───────────────────────┘
                          ▼
                 ┌─────────────────┐
                 │     place       │
                 │ (최종 선택 장소) │
                 └─────────────────┘
```

---

## 📝 테스트

```bash
# 테스트 실행
uv run python test_place_search.py -t preference_subway

# 옵션
# -t center_location    : 중간위치 방식
# -t preference_area    : 선호 지역 방식
# -t preference_subway  : 선호 지하철역 방식
```

---

## 📚 관련 문서

- [Kakao Local API 문서](https://developers.kakao.com/docs/latest/ko/local/dev-guide)
- [Google Gemini API 문서](https://ai.google.dev/docs)

---

## 🔮 향후 개선 사항

- [ ] 영업시간과 모임 시간 매칭
- [ ] 예약 가능 여부 실시간 확인
- [ ] 사용자 피드백 기반 추천 개선
- [ ] 1차/2차 장소 연계 추천

